<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".BaseDatosActivity"
            android:exported="false" />

        <activity
            android:name=".Main_login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Mantener MainActivity como secundaria -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Agregar la declaración para la actividad de descripción de películas -->
        <activity
            android:name=".MovieDescriptionActivity"
            android:exported="true" /> <!-- Agregar la declaración para la actividad de configuraciones -->
        <activity
            android:name=".Main_configures"
            android:exported="true" /> <!-- Agregar la declaración para la actividad de compra de boletos -->
        <activity
            android:name=".Mainboleto"
            android:exported="true" />
        <activity
            android:name=".RestablecerContrasenaActivity"
            android:exported="true" />
        <activity
            android:name=".Mainpaw"
            android:exported="true" />
        <activity
            android:name=".Mainreserva"
            android:exported="true" />
        <activity
            android:name=".MainGestor"
            android:exported="true" />
        <activity
            android:name=".MainTicket"
            android:exported="true" />
        <activity
            android:name=".Main_correo"
            android:exported="true" />
    </application>

</manifest>